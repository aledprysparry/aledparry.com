/** Cwis Bob Dydd brand tokens - shared across all canvas renderers */

// Royal-indigo + gold, matched to the brand halftone art (see brandPaint).
export const BRAND_BLUE = '#231062'; // deep indigo - reads on gold/silver/bronze + as ink
export const BRAND_YELLOW = '#ffd60a';
export const BRAND_WHITE = '#ffffff';
export const BRAND_RED = '#b91c1c'; // Welsh dragon red (primary)

// Self-hosted Rubik (body) + Digitalt display when its woff2 is present;
// the canvas falls back per the stack, so these match the Weekly Scoreboard.
export const DEFAULT_FONT = 'Rubik, Inter, sans-serif';
export const SERIF_FONT = 'Digitalt, Rubik, Inter, sans-serif';
